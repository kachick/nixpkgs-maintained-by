rules:
  unpinned-uses:
    config:
      policies:
        kachick/*: any
        actions/*: any
        NixOS/*: any
        crate-ci/*: any
  excessive-permissions:
    ignore:
      - merge-bot-pr.yml
